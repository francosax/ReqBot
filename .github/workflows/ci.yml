name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, 'claude/**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libgl1 \
          libglib2.0-0 \
          libdbus-1-3 \
          libxkbcommon-x11-0 \
          libxcb-icccm4 \
          libxcb-image0 \
          libxcb-keysyms1 \
          libxcb-randr0 \
          libxcb-render-util0 \
          libxcb-xinerama0 \
          libxcb-shape0 \
          libegl1

    - name: Cache pip packages and spaCy models
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/pip
          /opt/hostedtoolcache/Python/${{ matrix.python-version }}*/x64/lib/python*/site-packages
        key: ${{ runner.os }}-py${{ matrix.python-version }}-deps-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-py${{ matrix.python-version }}-deps-

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --only-binary=blis,thinc,cymem,preshed,murmurhash -r requirements.txt
        pip install pytest pytest-qt pytest-cov pytest-timeout

    - name: Download spaCy model
      run: |
        python -m spacy download en_core_web_sm

    - name: Run unit tests
      env:
        QT_QPA_PLATFORM: offscreen
      run: |
        pytest tests/test_gui.py -v -s --tb=short --timeout=300 --cov=main_app --cov-report=xml --cov-report=term

    - name: Run integration tests
      env:
        QT_QPA_PLATFORM: offscreen
      run: |
        pytest tests/test_integration_dragdrop.py -v -s --timeout=600
        pytest tests/test_integration_progress_details.py -v -s --timeout=600

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install linting tools
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black isort

    - name: Run flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --statistics

    - name: Check code formatting with black
      run: |
        black --check --diff .
      continue-on-error: true

    - name: Check import sorting with isort
      run: |
        isort --check-only --diff .
      continue-on-error: true

  build:
    name: Build Check
    runs-on: ubuntu-latest
    needs: [test, lint]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Compile Python files
      run: |
        python -m compileall .

    - name: Check for syntax errors
      run: |
        python -m py_compile *.py
